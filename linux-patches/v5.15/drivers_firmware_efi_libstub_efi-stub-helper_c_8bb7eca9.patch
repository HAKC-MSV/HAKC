diff --git a/drivers/firmware/efi/libstub/efi-stub-helper.c b/drivers/firmware/efi/libstub/efi-stub-helper.c
index d489bdc645fe..d63b8495fb34 100644
--- a/drivers/firmware/efi/libstub/efi-stub-helper.c
+++ b/drivers/firmware/efi/libstub/efi-stub-helper.c
@@ -160,7 +160,11 @@ int efi_printk(const char *fmt, ...)
 	fmt = printk_skip_level(fmt);
 
 	va_start(args, fmt);
+#if IS_ENABLED(CONFIG_HAKC)
+	printed = ls_vsnprintf(printf_buf, sizeof(printf_buf), fmt, args);
+#else
 	printed = vsnprintf(printf_buf, sizeof(printf_buf), fmt, args);
+#endif
 	va_end(args);
 
 	efi_puts(printf_buf);
@@ -409,8 +413,13 @@ char *efi_convert_cmdline(efi_loaded_image_t *image, int *cmd_line_len)
 	if (status != EFI_SUCCESS)
 		return NULL;
 
+#if IS_ENABLED(CONFIG_HAKC)
+	ls_snprintf((char *)cmdline_addr, options_bytes, "%.*ls",
+		 options_bytes - 1, options);
+#else
 	snprintf((char *)cmdline_addr, options_bytes, "%.*ls",
 		 options_bytes - 1, options);
+#endif
 
 	*cmd_line_len = options_bytes;
 	return (char *)cmdline_addr;
